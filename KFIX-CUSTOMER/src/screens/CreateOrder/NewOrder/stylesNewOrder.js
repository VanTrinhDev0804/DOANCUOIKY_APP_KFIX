import { StyleSheet } from 'react-native'
import { colors } from '../../../contains'

const stylesNewOrder = StyleSheet.create({
    wConstainLocatin: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    txtLocation: {
        fontSize: 20,
        color: colors.textColor,
    }
})

export default stylesNewOrder